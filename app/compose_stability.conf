// Este archivo fuerza al compilador de Compose a tratar estas clases como estables.
// Esto permite que Compose "salte" (skip) la recomposición si los datos no han cambiado.

// Modelos principales de PixelPlay
rve.pixelplay.data.model.Song
rve.pixelplay.data.model.Artist
rve.pixelplay.data.model.Album
rve.pixelplay.data.model.Lyrics
rve.pixelplay.data.model.MusicFolder
rve.pixelplay.data.model.Genre

// Clases de estado que aparecían como Runtime o Unstable
rve.pixelplay.presentation.viewmodel.PlayerUiState
rve.pixelplay.presentation.viewmodel.StablePlayerState
rve.pixelplay.presentation.components.BottomNavItem
rve.pixelplay.presentation.viewmodel.LyricsSearchUiState
rve.pixelplay.presentation.viewmodel.PickResult

// Clases externas comunes que a veces causan problemas
kotlinx.coroutines.flow.StateFlow
kotlinx.coroutines.flow.MutableStateFlow
com.google.common.util.concurrent.ListenableFuture

// También puedes estabilizar paquetes completos si confías en ellos
rve.pixelplay.data.model.**